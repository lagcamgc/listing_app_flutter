class Type {
    final int id;
    final String name;
    final String icon;
    final int hasPrice;
    final int hasPhones;
    final int hasAddresses;
    final int hasSocialNetworks;

    const Type({
        required this.id,
        required this.name,
        required this.icon,
        required this.hasPrice,
        required this.hasPhones,
        required this.hasAddresses,
        required this.hasSocialNetworks,
    });
}